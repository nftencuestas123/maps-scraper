import requests
from bs4 import BeautifulSoup

def run_scraper(country, city, neighborhood):
    # Tu lógica de scraping aquí
    # Devuelve una lista de resultados
    return [{"name": "Sample Business", "address": "Sample Address"}]
